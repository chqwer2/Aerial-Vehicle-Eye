

# Using CIL
# yolo TASK MODE ARGS

# TASK (optional) is one of [detect, segment, classify]
# MODE (required) is one of [train, val, predict, export]
# ARGS (optional) are task-specific arguments pairs like imgsz=320
# that override defaults. For a full list of available ARGS see

